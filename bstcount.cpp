#include<stdio.h>
struct node{
    int val;
    node* left,right;
};

class tree(){
    node* root;
    tree() {
        root = 0;
    }
}
main() {
    int n;
    scanf("%d",&n);
    while(n--) {

    }
}